# 项目库删除与 0.1.3 实施计划

1. 先为服务层补失败测试：正确删除、确认不匹配、revision conflict、活动任务、活动工作台会话、外部源文件保留。
2. 实现精确目标解析、活动状态检查、同盘暂存清理和 DELETE API；运行项目 API 聚焦测试。
3. 先更新静态 UI 测试，再实现卡片/列表选择、页面内二次确认、批量 DELETE 和错误反馈。
4. 同步 main 最新提交，解决冲突时保留 main 的质量检测进度修复和当前分支的场景打通实现。
5. 将项目元数据、运行时版本和 Windows 离线包配置统一到 0.1.3，并更新相应测试。
6. 运行项目删除、项目库、打包聚焦测试，全量 pytest、依赖检查及 `git diff --check`。
7. 使用一次性项目做真实 HTTP 删除 smoke，确认项目消失且外部源文件仍存在。
8. 分阶段提交后本地合并 main；不推送、不删除功能工作树。
