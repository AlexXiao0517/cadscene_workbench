# v0.1.0-road-sfm-only Baseline Report

Date: 2026-07-17

## Python environment

- Python: 3.11.7
- Executable: `D:\anaconda3\python.exe`
- Platform: Windows 10.0.26220-SP0
- pytest: 7.4.0

## Test results

- Required command: `python -m pytest`
- Result: collection stopped with 14 import errors from `project/tests/`, all caused by the unavailable legacy `cadvideo` package. The command collected 251 current-workbench tests before the legacy collection errors.
- Workbench-only confirmation: `python -m pytest tests`
- Result: **251 passed**, 1 third-party deprecation warning, in 26.77 seconds.

The `project/` tree is an intentionally excluded read-only legacy reference and is not part of the v0.1.0 Git baseline.

## Independence check

- Command: `python scripts/check_no_project_dependency.py`
- Result: passed — no forbidden `project/` or `cadvideo` runtime dependencies found.

## CLI smoke checks

All of the following commands completed successfully:

- `python -m cadscene.cli.serve_viewer --help`
- `python -m cadscene.cli.run_sfm --help`
- `python -m cadscene.cli.run_pipeline --help`

## Known limitations

- The all-directory pytest invocation encounters legacy `project/tests` imports; use `python -m pytest tests` for the standalone workbench suite.
- pycolmap/COLMAP and other optional runtime capabilities are environment-provided rather than bundled by this baseline.
- CUDA acceleration is outside this v0.1.0 baseline.

## Unsupported modes

- Partial-SRT is not supported.
- Full-pose SRT interfaces are not supported.
- The stable workflow is the local-data, no-SRT SfM-to-CAD alignment workflow.
